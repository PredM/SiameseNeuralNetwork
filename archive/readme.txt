This directory contains code fragments not currently used in the implementation,
but could possibly be reused at some point the future.
Note that the code is most likely not up-to-date and therefore not directly compatible with the rest of the updated implementation.
It may also contain important examples/test implementations.